# Public Agents Package

This package is intentionally minimal.

- keep `skills/` empty in the public dotfiles repo unless a stable public skill
  is promoted back here
- private and in-progress skills live in
  `~/dev/agents/agents/.agents/skills/`
- use the dotfiles `make agents-enable-private` target to merge the private
  package into `~/.agents`
