# Dotfiles

Personal dotfiles repo managed with symlinks.

## Key paths

- `agents/.agents/` — shared agent skills and config, symlinked to `~/.agents`
  - `skills/` — reusable skills (e.g. `parley/`)
